# Secrets — device credentials live here, never commit
.env

# Rust build output (daemon)
daemon/target/

# .NET build output (OTD plugin + hiddiag tool)
otd-plugin/bin/
otd-plugin/obj/
tools/hiddiag/bin/
tools/hiddiag/obj/

# Compiled Qt resource bundle (regenerated by appload/deploy.py)
appload/resources.rcc

# Python
__pycache__/
*.py[cod]

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
